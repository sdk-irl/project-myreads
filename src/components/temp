      <div className="app">
        {this.state.showSearchPage ? (
          < Search 
            books={this.state.books}
            moveBook={this.moveBook}
          />
        ) : (
          < Main
            books={this.state.books}
            moveBook={this.moveBook}
          />
        )}
      </div>